import { BookStoreConstants } from './book-store-constants';

describe('BookStoreConstants', () => {
  it('should create an instance', () => {
    expect(new BookStoreConstants()).toBeTruthy();
  });
});
