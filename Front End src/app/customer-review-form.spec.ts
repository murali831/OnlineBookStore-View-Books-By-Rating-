import { CustomerReviewForm } from './customer-review-form';

describe('CustomerReviewForm', () => {
  it('should create an instance', () => {
    expect(new CustomerReviewForm()).toBeTruthy();
  });
});
